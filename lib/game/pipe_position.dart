/// The pipes can be in 2 positions [PipePosition.top] or [PipePosition.bottom].
enum PipePosition {
  /// The pipe is at the top of the screen.
  top,

  /// The pipe is at the bottom of the screen.
  bottom,
}
